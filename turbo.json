{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "lint": {},
        "lint:fix": {},
        "format": {},
        "format:fix": {},
        "test": {
            "dependsOn": ["build"]
        },
        "coverage": {},
        "build": {
            "dependsOn": ["^build"]
        }
    },
    "globalDependencies": [".eslintrc", ".prettierrc"]
}
